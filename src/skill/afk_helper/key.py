from skill.afk_helper.size import size

# 挑战首领
challenge_boss = {"name": "挑战首领", "point": (size.width(356), size.height(1088))}
# 弹出上的挑战首领(有时候会出现)
second_challenge_boss = {"name": "弹窗上的挑战首领", "point": (size.width(356), size.height(1223))}
# 战斗
battle = {"name": "战斗", "point": (size.width(356), size.height(1223))}
# 再次挑战
retry = {"name": "再次挑战", "point": (size.width(356), size.height(1140))}
